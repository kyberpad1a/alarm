<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/edtSeconds"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Укажите количество секунд"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <Button
        android:id="@+id/btnStartTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Установить будильник"
        app:layout_constraintLeft_toLeftOf="@id/edtSeconds"
        app:layout_constraintRight_toRightOf="@id/edtSeconds"
        app:layout_constraintTop_toBottomOf="@id/edtSeconds" />

    <Button
        android:id="@+id/btnTime"
        android:layout_width="242dp"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        android:text="Изменить время"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnStartTime" />

    <Button
        android:id="@+id/btnDate"
        android:layout_width="244dp"
        android:layout_height="46dp"
        android:layout_marginTop="8dp"
        android:text="Изменить дату"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnTime" />

</androidx.constraintlayout.widget.ConstraintLayout>